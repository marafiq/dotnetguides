@inherits SliceComponent<SkillsListProps>

<div class="skills-list">
    <h4 class="skills-title">@Props.Title</h4>
    <div class="skills-container">
        @foreach (var skill in Props.Skills)
        {
            <SkillTag Name="@skill" />
        }
    </div>
</div>
