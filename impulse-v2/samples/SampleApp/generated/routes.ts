// Auto-generated by impulse-gen - DO NOT EDIT
// Virtual file routes for TanStack Router
import { createRoute, createRootRoute, createRouter } from '@tanstack/react-router';
import { RoutePaths } from './routePaths';
import type { ListResidentsResponse, GetResidentResponse, ListMedicationsResponse, GetMedicationResponse, GetCarePlanResponse } from './types';

// Context type for impulse loader
interface ImpulseContext {
  impulse: <T>(url: string) => Promise<T>;
}

// Root route
export const rootRoute = createRootRoute();

export const listResidentsRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/residents',
  loader: async ({ context, params }) => {
    const ctx = context as ImpulseContext;
    return ctx.impulse<ListResidentsResponse>(RoutePaths.ListResidents);
  },
});

export const getResidentRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/residents/$id',
  loader: async ({ context, params }) => {
    const ctx = context as ImpulseContext;
    let url: string = RoutePaths.GetResident;
    url = url.replace('{id}', params.id);
    return ctx.impulse<GetResidentResponse>(url);
  },
});

export const listMedicationsRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/residents/$residentId/medications',
  loader: async ({ context, params }) => {
    const ctx = context as ImpulseContext;
    let url: string = RoutePaths.ListMedications;
    url = url.replace('{residentId}', params.residentId);
    return ctx.impulse<ListMedicationsResponse>(url);
  },
});

export const getMedicationRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/residents/$residentId/medications/$medicationId',
  loader: async ({ context, params }) => {
    const ctx = context as ImpulseContext;
    let url: string = RoutePaths.GetMedication;
    url = url.replace('{residentId}', params.residentId);
    url = url.replace('{medicationId}', params.medicationId);
    return ctx.impulse<GetMedicationResponse>(url);
  },
});

export const getCarePlanRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: '/residents/$residentId/care-plan',
  loader: async ({ context, params }) => {
    const ctx = context as ImpulseContext;
    let url: string = RoutePaths.GetCarePlan;
    url = url.replace('{residentId}', params.residentId);
    return ctx.impulse<GetCarePlanResponse>(url);
  },
});

// Route tree - add all routes as children of root
export const routeTree = rootRoute.addChildren([
  listResidentsRoute,
  getResidentRoute,
  listMedicationsRoute,
  getMedicationRoute,
  getCarePlanRoute,
]);

// Create the router instance
export const router = createRouter({ routeTree });

// Route definitions object for programmatic access
export const routes = {
  listResidents: {
    path: '/residents' as const,
    method: 'GET' as const,
    loader: RoutePaths.ListResidents,
  },
  getResident: {
    path: '/residents/{id}' as const,
    method: 'GET' as const,
    loader: RoutePaths.GetResident,
  },
  listMedications: {
    path: '/residents/{residentId}/medications' as const,
    method: 'GET' as const,
    loader: RoutePaths.ListMedications,
  },
  getMedication: {
    path: '/residents/{residentId}/medications/{medicationId}' as const,
    method: 'GET' as const,
    loader: RoutePaths.GetMedication,
  },
  getCarePlan: {
    path: '/residents/{residentId}/care-plan' as const,
    method: 'GET' as const,
    loader: RoutePaths.GetCarePlan,
  },
} as const;

// Type-safe path builders
export function getResidentPath(id: string | number) {
  return `/residents/${id}` as const;
}
export function listMedicationsPath(residentId: string | number) {
  return `/residents/${residentId}/medications` as const;
}
export function getMedicationPath(residentId: string | number, medicationId: string | number) {
  return `/residents/${residentId}/medications/${medicationId}` as const;
}
export function getCarePlanPath(residentId: string | number) {
  return `/residents/${residentId}/care-plan` as const;
}
